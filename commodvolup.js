// Example arrays
const offset = [1, 2, 3];
const anchor = [4, 5, 6, 7, 8];

// Determine the larger length
const positions = Math.max(offset.length, anchor.length);

console.log(positions); // Output: 5
